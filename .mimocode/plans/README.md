# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `e73f337b`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                 | Priority | Effort | Depends on    | Status |
| ---- | ----------------------------------------------------- | -------- | ------ | ------------- | ------ |
| 001  | Close the Learning Loop — Lesson Progress Persistence | P1       | M      | —             | TODO   |
| 002  | Vocabulary → Flashcard Pipeline                       | P1       | M      | 001           | TODO   |
| 003  | Story Comprehension → Grading + Competency            | P1       | M      | 001           | TODO   |
| 004  | Lesson→Pronunciation + Dictionary Integration         | P2       | S      | 002           | TODO   |
| 005  | Cross-Feature Integration Loops (Wave 3)              | P2       | M      | 001, 002, 003 | TODO   |

**Total estimated effort**: 3-4 sessions (plans 001-003 can partially parallelize after 001 lands)

## Dependency notes

- 002 requires 001 because `vocabularyList` table lives in the same v33 migration block, and lesson vocabulary needs lesson persistence to be meaningful
- 003 requires 001 because `trackComprehensionResult` uses the same competency tracking infrastructure added in 001
- 004 requires 002 because pronunciation integration wires to saved vocabulary words
- 005 requires all of 001-003 because each integration loop connects two features that 001-003 establish

## Direction findings considered and rejected

- **Full i18n for all UI text**: High effort (L), low immediate value — the 25 remaining keys in af.json/zu.json are brand names and templates. Deferred to a dedicated i18n session.
- **Offline dictionary/stories bundle**: Medium effort (M) but depends on the lesson/vocabulary/story features being stable first. Deferred until Wave 2 features are shipped.
- **P0 runtime crashes (9 pages HTTP 000)**: Investigated during recon — the `createOfflineDBProxy()` in `src/lib/db/schema.ts:1188-1207` already returns a no-op proxy when `typeof window === "undefined"`, which should prevent the SSR Dexie crash. The infinite re-render in `useOnboarding` was also already fixed (callbacks use functional updaters with `[]` deps). These P0s appear resolved.
- **Dead barrel files (22 files)**: Cleanup task, not a direction finding. Low value relative to feature work.
- **ResultsSearch mock data**: The `mockExamResults` in `results-search.tsx` shows a "Demo data" badge — this is a known limitation, not a hidden bug.
