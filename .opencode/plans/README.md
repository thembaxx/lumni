# Implementation Plans — Batch 10 (July 2026 deep audit)

Generated by the improve skill on 2026-07-07. Commit baseline: `649afc3b`.

All plans in `.opencode/plans/` — see `plans/README.md` for batches 1-9 (plans 001-132).

## Execution order & status — Batch 10 (deep audit: security, bugs, perf, tests, architecture)

Phase 1 (P0) plans first. Phases 2-6 are independent and can run in any order within each phase.

| Plan | Title                                                                        | Priority | Effort | Risk | Depends on | Status |
| ---- | ---------------------------------------------------------------------------- | -------- | ------ | ---- | ---------- | ------ |
| 133  | Gate CSP 'unsafe-eval' behind NODE_ENV in next.config.ts                     | P0       | S      | LOW  | —          | DONE   |
| 134  | Add auth guards to three unprotected endpoints                               | P0       | S      | LOW  | —          | DONE   |
| 135  | Limit base64 payload size in transcribe endpoint                             | P1       | S      | LOW  | —          | DONE   |
| 136  | Set explicit maxFileSize on all UploadThing uploaders                        | P1       | S      | LOW  | —          | DONE   |
| 137  | Add enrollment check to assignment submit endpoint                           | P0       | S      | LOW  | —          | DONE   |
| 138  | Fix exam auto-save — add sendBeacon on page unload                           | P1       | S      | LOW  | —          | DONE   |
| 139  | Fix hardcoded questionType in analytics events                               | P1       | S      | LOW  | —          | DONE   |
| 140  | Fix PdfCache blob URL leak in clearOld + getUrl                              | P1       | S      | LOW  | —          | DONE   |
| 141  | Fix quiz FINISH race — guard against last RECORD_ANSWER in flight            | P1       | S      | LOW  | —          | DONE   |
| 142  | Replace empty .catch(() => {}) with logError across codebase                 | P1       | M      | LOW  | —          | DONE   |
| 143  | Fix 4 full-table Dexie scans — use indexed where() queries                   | P1       | S      | LOW  | —          | DONE   |
| 144  | Consolidate recharts imports — dynamic per-page, single import per chart     | P1       | S      | LOW  | —          | DONE   |
| 145  | Extract shared syntax-highlighter lazy-loader singleton                      | P1       | S      | LOW  | —          | DONE   |
| 146  | Add achievement engine unit tests                                            | P1       | M      | LOW  | —          | DONE   |
| 147  | Configure vitest coverage thresholds and add coverage CI step                | P1       | S      | LOW  | —          | DONE   |
| 148  | Add InMemoryDataAccess unit tests                                            | P1       | S      | LOW  | —          | DONE   |
| 149  | Add webhook handler tests (SendGrid + Sentry)                                | P1       | M      | LOW  | —          | DONE   |
| 150  | Fix notification scheduling timer leak — clear cascading timeouts on unmount | P1       | M      | LOW  | —          | DONE   |
| 151  | Fix gamification dual-write race — atomic modify instead of read-then-write  | P1       | M      | LOW  | —          | DONE   |
| 152  | Deduplicate Dexie schema type exports — prune unused barrel exports          | P2       | S      | LOW  | —          | DONE   |
| 153  | Remove legacy spaced-repetition barrel re-export shims                       | P2       | S      | MED  | —          | DONE   |
| 154  | Add input debounce to dashboard SearchWidget + dedupe results                | P2       | S      | LOW  | —          | DONE   |
| 155  | Extract dashboard god hook — split into domain-specific hooks                | P2       | L      | LOW  | —          | DONE   |
| 156  | Create localStorage abstraction layer — migrate remaining direct accesses    | P2       | L      | MED  | —          | DONE   |

## Priority adjustments

- Plans 130-131 (from Batch 9) bumped from P2→P1 and P1→P1 respectively

## Execution notes

### Phase 1 — P0 security (plans 133, 134, 137)

Three independent P0 fixes for CSP bypasses and unauthenticated routes. Execute first.

### Phase 2 — P1 bugs (plans 135-136, 138-142)

Six independent bug fixes. Execute in any order after Phase 1. Plans 135-136 were already executed during the audit conversation.

### Phase 3 — P1 performance (plans 143-145)

Three independent performance fixes. Full-table Dexie scans (143) is the highest-impact for users with large datasets.

### Phase 4 — P1 test coverage (plans 146-149)

Four independent test-coverage plans. Plan 147 (coverage thresholds) should run after the others since the coverage numbers will be higher.

### Phase 5 — P1 M-effort bugs (plans 150-151)

Timer leak and race condition fixes. Independent of other phases.

### Phase 6 — P2 architecture + perf (plans 152-156)

Lower priority. Plans 155-156 are L-effort and should be scheduled when there's dedicated architecture time. Plan 153 (shim removal) has MED risk — verify with `rg`, not just typecheck.

## Dependency graph

```
Phase 1 ──► Phase 2 ──► Phase 3 ──► Phase 4 ──► Phase 5 ──► Phase 6
 (all independent within each phase)
```

## Vetting notes

All findings verified against actual code at commit `649afc3b`. Rejected findings:

- SEC-13 "CSP unsafe-inline" — by-design for Next.js style injection
- DIR-01 "sync disconnected" — false positive, sync is wired
- ARCH-01 "dual barrel" — false positive, barrel re-exports are intentional
- Several other subagent-misattributed findings rejected after manual verification

## Plans moved to `.opencode/plans/`

Write-denied on `plans/` for agent tooling. Plans 133-156 live in `.opencode/plans/`.
