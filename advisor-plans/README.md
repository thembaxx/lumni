# Advisor Implementation Plans

Generated by the `improve` skill on 2026-07-09 from commit `be3a4dfb`. The `plans/` directory already exists with 145 plans from previous sessions; these 7 plans are placed in `advisor-plans/` to avoid collision.

Execute in the order below unless dependencies say otherwise. Run the drift check (`git diff --stat be3a4dfb..HEAD -- <in-scope>`) before each plan.

## Execution order & status

| Plan | Title                                                 | Priority | Effort | Depends on | Status |
| ---- | ----------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Add Origin header validation to `createRouteHandler`  | P1       | S      | —          | TODO   |
| 002  | Fix 3 unauthenticated API endpoints to require auth   | P1       | S      | —          | TODO   |
| 003  | Memoize gamification hook return value                | P1       | S      | —          | TODO   |
| 004  | Replace full-table Dexie scans with indexed queries   | P1       | S      | —          | TODO   |
| 005  | Sweep empty catch blocks → structured error logging   | P2       | S      | —          | TODO   |
| 006  | Raise vitest coverage thresholds to meaningful values | P2       | S      | —          | TODO   |
| 007  | Add SSRF guard to admin batch-extract endpoint        | P2       | S      | —          | TODO   |

## Dependency notes

- No plan depends on another. All 7 are independent and can be executed in any order, though recommended order is P1→P2.

## Findings considered and rejected

- **SEC-01 (study-sessions unauthenticated writes)** — covered by plan 002 (which also fixes re-engagement and exam-dates).
- **CORRECTNESS-02 (exam timer side effects during render)** — valid finding but higher risk to fix (changing timing logic could break exam session flow). Rejected for this batch; recommend a separate targeted plan.
- **PERF-03 (Konva theme toggle full re-render)** — would require a shared `DiagramThemeProvider` context. Valid but effort is larger than benefit; dismissed for now.
- **PERF-04 (dashboard dual polling)** — the 30s/60s poll issue is user-facing but the fix is trivial (change `refetchInterval` to `false`). Too simple to warrant a plan — it's a one-line change that can be done ad-hoc.
- **DM-01 (Next.js preview release)** — can't fix without stable 16.3.0 tag. Pending external event.
- **DM-02 (dead dependencies)** — `react-day-picker` removal and README fixes are real but P3 priority. Dismissed for this batch.
- **TD-01 (68 files importing dexieDataAccess directly)** — high-impact but also high-risk. Requires a dedicate session with per-file review. Too large for a safe plan in this batch.
- **TD-03 (orchestrator sync/domain handler tests)** — valid, but characterization tests first, then split. This batch focuses on high-leverage security/perf fixes instead.
- **DIR-01 (OCR timetable scraping)** — the plan already exists at `plans/103-ocr-exam-timetable-scraping.md`. No need to duplicate.
- **CORRECTNESS-03 (flashcard race condition)** — confirmed but requires Dexie transactions. Rejected for this batch; recommend as `P3` follow-up.
